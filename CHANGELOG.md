# 更新记录

版本号记录 ComfyUI-Soter-Nodes 的改版，不代表原项目官方版本。

## 2.2.1 — 2026-09-11

- 增加“版权”和“角色名”分类，新建节点默认启用全部 14 类。`arknights` 归入版权，`suzuran_(arknights)`、`vulpisfoglia_(arknights)` 归入角色名。
- 随附 73,171 个 Danbooru 版权和角色规范名称的离线索引；新增默认开启的 `danbooru_lookup`，对本地未识别的 tag 补查 D 站类别并缓存，查询失败时回到默认分类。
- Aaalice Booru Gallery 的 `prompts` 直连 Packer 时，优先复用与当前图片提示词匹配的分类数据，按实际选择、排除和编辑后的 tag 处理，多图分类不混用；缺少或无法匹配数据时继续使用本地和在线分类，无需修改 Gallery 或新增连线。
- 保留通用 `STRING` 输入，WD14 等反推节点、其他文本节点和手动输入均可使用本地分类与在线补查，不依赖 Gallery。
- 分类查找兼容 Gallery 的空格替换下划线和转义括号，同时保留输出 tag 的原始拼写；显式自定义映射优先于 Gallery、离线索引和 D 站补查。
- 旧工作流自动补齐两类缺失映射，保留显式自定义映射；新增分类行默认关闭，需手动开启并排序。
- 自定义映射重命名版权、角色分类时，面板使用自定义名称，不再额外补入未使用的默认名称行。
- `force_reload` 和清缓存节点同时清除在线分类查询缓存。
- 新建 Packer 的 `is_comment` 默认关闭，`ALL_TAGS` 默认只输出所选分类的 tag，按分类列表顺序排列，不添加分类标题。
- `is_comment` 开关显示为 `仅tag` / `含分类名`；输出标签改为 `分类数据包·Getter` 和 `ALL_TAGS·已选tag`，明确结构化分类数据与提示词文本的用途。
- 保留原输出名称、插槽顺序、类型及 Getter 接口；已有工作流保存的开关值和连线不变，升级后需按需切换为 `仅tag` 并改接 `ALL_TAGS·已选tag`。
- README 增加纯 tag 接线步骤、示例及通用预览节点的 Unicode 转义说明。预览中出现的 `\u955c\u5934\u8bcd` 是字典中中文分类名的 JSON 显示形式；纯 tag 提示词应使用 `ALL_TAGS`。
- 在线查询的自动测试使用模拟接口；开发环境直连 Danbooru 超时，真实在线查询仍待实际环境验证。
- 更新 14 类控件预览图和示例工作流。59 项 Python 测试、15 项浏览器交互场景通过（Node TAP 含父测试共 16 项）；实际加载随附 Excel，并与上游 Gallery 的提示词组合函数对照 100 组输入。浏览器测试使用模拟的 ComfyUI 接口，本次完整改动待实际 ComfyUI 环境验证。

## 2.2.0 — 2026-09-11

### 新增

- 在 Packer 原分类顺序文本框位置加入分类选择器：逐行开关、全部启用/关闭、tag 预览、右侧分类名、上下箭头与拖动排序。
- 新建节点完整显示并启用默认 12 类；映射中的其他分类自动补齐，旧配置遗漏项和新加入项默认关闭。
- 分类开关和顺序随工作流保存；支持键盘操作、长 tag 展开和输入变化后的预览更新提示。
- 增加 `new_category_order` 的 JSON 对象数组格式（`name` / `enabled`），通过 ComfyUI 的 `ui` / `result` 响应传回预览；保留原 `STRING` 输入及输出插槽。
- 发布完整源码、内置数据库、默认配置、控件预览图、测试及安装迁移说明。

### 行为与修复

- 分类数据包和 `ALL_TAGS` 按列表顺序仅输出启用项；关闭分类中的 tag 不再回流默认分类。
- “未归类词”遵循用户设置的位置及开关；启用的空分类保留为空字符串，全部关闭时输出空字典及空文本。
- 开关和排序复用已加载数据库，避免无意义的 Excel 重读；修改映射或强制重载仍刷新相关缓存。
- 原位替换控件，保留旧工作流其他控件的序列化索引；清理旧 DOM 生命周期，兼容输入转换与移除接口差异。
- 在修改前后通知图事务，以支持撤销/重做；预览更新和工作流恢复不产生编辑事务。
- 接线上游时使用只读预览，不覆盖本地保存的选择；断开后恢复本地配置。
- Getter 继续接收普通 `TAG_BUNDLE` 字典；旧名称数组的直接 API 调用保留原返回形式、校验及默认分类行为。
- 保留原节点注册名称、原作者署名和 MIT 许可证；内置数据库使用可迁移的相对文件名。

### 验证

- 19 项 Python 测试通过，覆盖筛选与排序、默认分类、空值、黑名单/去重、Getter、缓存及旧 API 兼容。
- 12 项真实浏览器交互场景通过（Node TAP 含父测试共 13 项），覆盖选择器交互、恢复、连线和 ComfyUI 适配行为。
- 实际加载随附 Excel，索引 221,784 个 tag。
- 浏览器测试使用模拟的 ComfyUI 节点及图接口；尚未在用户完整 ComfyUI 环境中实测。测试命令及试用步骤见 [README](README.md)。

本次改版基于 [RafealaSilva/ComfyUI-Danbooru-Tag-Sorter-Node](https://github.com/RafealaSilva/ComfyUI-Danbooru-Tag-Sorter-Node) 提交 `8957613905488033f8297e246c5e1adfbe387560`。
